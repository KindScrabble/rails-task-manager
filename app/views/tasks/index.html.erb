<div class="container">
<h1>My tasks</h1>

<% @tasks.each do |task| %>
  <div class="category">
      <% if task.completed? %>
        <i class="fas fa-check"></i>
      <% else %>
        <i class="far fa-square"></i>
    <% end %>
    <p> <%= task.title %> </p>
  </div>
<% end %>
</div>
